<tr>

  <td>
  <%#= realty.street + " " + (realty.number_unit).to_s + " " + realty.unit_estate+ " " + realty.commune.name %>
<%#= "#{realty.street} #{(realty.number_unit).to_s}  #{realty.unit_estate}  #{realty.commune.name}" %>
    <%= realty.address %>
  </td>
  <td> <%= "#{realty.unit_estate}, #{realty.property_type}"%></td>


  <td>
    <%= link_to 'Show', realty %>
    <%= link_to 'Edit', edit_realty_path(realty) %>
    <%= link_to 'Destroy', realty, method: :delete, data: { confirm: 'Are you sure?' } %>
  </td>
</tr>
